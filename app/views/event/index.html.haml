%h1 Eventos cadastrados
%table
  %tr
    %th Evento
    %th Info
    %th
  - @events.each do |ev|
    %tr
      %td= ev.nome
      %td= ev.info
      %td
        = link_to t(:delete), event_path(ev), method: :delete,
      data: {confirm: "Você tem certeza?"}
      %td
        = link_to t(:show), event_path(ev)
        = link_to t(:edit), edit_org_path(ev)
= link_to t(:back), new_event_path
